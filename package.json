{
  "name": "sabaayloo",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "seed": "npm run seed --prefix backend -- ./toilets_bangkok.json",
    "install:all": "npm i --prefix backend && npm i --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
